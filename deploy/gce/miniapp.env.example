# Apply only to the production bot, after getMe confirms its identity.
# This is an activation preset, not an automatically loaded configuration.
TELEGRAM_MINI_APP_URL=https://galerazo.com/miniapp
TELEGRAM_MINI_APP_SHORT_NAME=hisopos
MINI_APP_BIND_HOST=127.0.0.1
MINI_APP_PORT=8080
# Generate secrets.token_urlsafe(32) privately; store the identical value in the Worker.
MINI_APP_PROXY_SECRET=
